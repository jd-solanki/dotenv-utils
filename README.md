# envolix

envolix helps keep Example Environment Files aligned with Source Environment Files without exposing source values.

## Development

Run the CLI locally:

```sh
pnpm envolix --help
pnpm envolix sync --help
```

Run tests:

```sh
pnpm test
```

The `sync` command is intentionally a non-success placeholder until the Sync implementation lands.
